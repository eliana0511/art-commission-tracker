# art-commission-tracker
Simple Python program that manages art commissions using Stack and Queue concepts (LIFO + FIFO).

# 🎨 Art Commission Tracker

A simple Python program that manages art commissions using **Stacks (LIFO)** and **Queues (FIFO)**.

## 💡 Features
- Add new art commissions to a waiting list (Queue)
- Mark commissions as completed (Stack)
- Undo the last completed commission
- View both waiting and completed lists
- Simple, one-file Python project

## 🧠 Concepts Demonstrated
- **Queue (FIFO)** → Tasks processed in order
- **Stack (LIFO)** → Undo feature / reverse order
- Demonstrates real-world data structure usage

## 🖥️ How to Run
```bash
python3 art_commission_tracker.py

Author
Ally Bazan


Then add + push it too:

```bash

git add README.md
git commit -m "Add README for Art Commission Tracker"
git push
